# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CoupleDictionary' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
pod 'Firebase/Core'
pod 'Firebase/Auth'
pod 'SwiftLint'
  # Pods for CoupleDictionary

  target 'CoupleDictionaryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CoupleDictionaryUITests' do
    # Pods for testing
  end

end
